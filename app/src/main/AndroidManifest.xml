<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chinna.deliveryapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/food3"
        android:label="@string/app_name"
        android:roundIcon="@drawable/food3"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:hardwareAccelerated="false"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SigninActivity"
            android:label="@string/title_activity_signin"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TermsCondtions"
            android:label="@string/title_activity_terms_condtions"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CustomlListActivity"
            android:label="@string/title_activity_customl_list"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>